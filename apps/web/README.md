# OpenDevX Web

This directory contains the Vite, React, and TypeScript scaffold for OpenDevX. Sprint 1 establishes the project structure only; no routing, API calls, dashboard views, or product features are included.

## Local development

```powershell
npm install
npm run dev
```
